{
  "name": "gitdocify",
  "version": "1.0.0",
  "description": "GitHub App that auto-generates README.md files using Claude AI",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "chmod +x deploy.sh && ./deploy.sh",
    "logs": "tail -f logs/app.log"
  },
  "keywords": ["github", "app", "readme", "ai", "claude", "automation"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "@octokit/rest": "^20.0.2",
    "@octokit/auth-app": "^6.0.1",
    "crypto": "^1.0.1",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}